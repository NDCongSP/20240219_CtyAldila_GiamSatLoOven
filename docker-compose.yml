version: '3.8'

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: oven-sqlserver
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=Dev@12345Strong!
      - MSSQL_PID=Developer
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
      - ./sourceCode/DB:/db-backup:ro
    networks:
      - oven-network
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "Dev@12345Strong!" -C -Q "SELECT 1" || exit 1
      interval: 10s
      timeout: 3s
      retries: 10
      start_period: 20s

volumes:
  sqlserver_data:
    driver: local

networks:
  oven-network:
    driver: bridge
